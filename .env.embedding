# Alternative embedding models for faster loading
# Uncomment one of these to use a different model

# Option 1: Smaller German model (recommended for faster startup)
# EMBEDDING_MODEL=sentence-transformers/distiluse-base-multilingual-cased-v1

# Option 2: Fast English/German model
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Option 3: German-specific model
# EMBEDDING_MODEL=deepset/gbert-base-germandpr-reranking